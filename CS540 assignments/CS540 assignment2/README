Description:
    
    Implement a Template container Map, acting like std::map；
    
    （1）Used doubly-linked Skip List to improve performance on random access from O(n) to O(log n);
    
    （2）Implemented Iterator, ConstIterator, ReverseIterator for Iterations and make them comparable and assignable.

How to run the code:

compile:
    g++ test1.cpp -std=c++11
Run:
    ./a.out -p > output.txt
Result saved in output.txt


